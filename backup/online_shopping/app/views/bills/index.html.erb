<div class="container" style="margin-top: 75px;">
  <div class="row">
  <div class="col-md-12">
      <div class="" style="background-color: #424242; width=70px;height: 40px; margin-top: 20px;">
        <h2 class="header" style="background-color: #424242; height: 38px;">
          <span style="text-transform: uppercase; font-size: 16px; color: #ffffff; margin-left: 10px;">Listing bills</span>
          <span style="font-size: 14px; color: #ffffff; margin-right: 10px; float: right; margin-top: 12px;"></span>
        </h2>
      </div>
      <br>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Bill id</th>
            <th>User Name</th>
            <th>Date</th>
            <th>Total Amount</th>
          </tr>
        </thead>

        <tbody>
          <% @bills.each do |bill| %>
            <tr>
              <td><%= bill.id %></td>        
              <% if bill.user %>
              <td><%= bill.user.name %></td>
              <% else %>
              <td>Nill </td>
              <% end -%>
              <td><%= bill.date %></td>
              <td><%= bill.total_amount %></td>
              <td><%= link_to 'Show', bill_path(bill) %></td>
              <td><%= link_to '', bill, method: :delete, data: { confirm: 'Are you sure?' }, :class=>"glyphicon glyphicon-trash",:title=>"delete"%></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>
  </div>
    <div class="col-md-2"></div>
  </div>
</div>
